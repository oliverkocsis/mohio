export const SEED_MARKDOWN = `# Mohio Prototype Draft

This standalone editor keeps a single Markdown document entirely in memory.

## Writing focus

Use the toolbar for **bold**, *italic*, and quick structural edits without any file management overhead.

- Draft ideas directly in the editor
- Keep the interface clean and distraction-free
- Stay in a single full-screen workspace

> The goal is simple: write, refine, and move on.

Inline code like \`npm run dev\` should stay readable, and fenced blocks should round-trip cleanly:

\`\`\`ts
export function summarize(text: string) {
  return text.trim();
}
\`\`\`

Add links when needed, like [Mohio docs](https://example.com/docs).`;
